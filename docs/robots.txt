# robots.txt for markdownstudio.dev
#
# Markdown Studio's whole premise is "draft with AI, file with confidence", so
# AI assistants are explicitly welcome to read, cite, and answer questions from
# this site. Everything here is public documentation for a source-available app.
#
# NOTE: this file is served through Cloudflare, whose "Managed robots.txt"
# feature will REPLACE it with a block that disallows every major AI crawler.
# That feature was turned off on 2026-08-10 (AI Crawl Control -> Signals), so
# this file is what actually gets served. If you ever see a
# "BEGIN Cloudflare Managed content" marker or a Disallow line in the live
# response, it has been re-enabled — see docs/RELEASING.md.

# ---------------------------------------------------------------------------
# Everyone
# ---------------------------------------------------------------------------
User-agent: *
Allow: /

# NOTE: a `Content-Signal:` directive used to sit here declaring
# search=yes,ai-input=yes,ai-train=yes,use=full. It was removed on 2026-08-19
# because it is not an RFC 9309 directive: robots.txt validators (Lighthouse
# among them) report the whole file as invalid over it, which cost the site's
# Lighthouse SEO score. Conforming crawlers ignore unknown directives, so it
# bought nothing that the explicit per-crawler `Allow:` rules below do not
# already say — and those are what crawlers actually obey.
#
# The spec-correct home for that signal is the `Content-Signal` HTTP response
# header, which GitHub Pages cannot set. It could be added with a Cloudflare
# response-header Transform Rule if the declaration is ever wanted back.

# ---------------------------------------------------------------------------
# AI crawlers — explicitly allowed
#
# Two kinds matter: crawlers that build training/index corpora, and
# user-triggered fetchers that retrieve a page because a human asked about it.
# Both are welcome.
# ---------------------------------------------------------------------------

# Anthropic
User-agent: ClaudeBot
Allow: /

User-agent: Claude-User
Allow: /

User-agent: Claude-SearchBot
Allow: /

User-agent: anthropic-ai
Allow: /

# OpenAI
User-agent: GPTBot
Allow: /

User-agent: OAI-SearchBot
Allow: /

User-agent: ChatGPT-User
Allow: /

# Google (Gemini / AI Overviews grounding)
User-agent: Google-Extended
Allow: /

User-agent: GoogleOther
Allow: /

# Microsoft / Copilot
User-agent: bingbot
Allow: /

# Perplexity
User-agent: PerplexityBot
Allow: /

User-agent: Perplexity-User
Allow: /

# Apple
User-agent: Applebot
Allow: /

User-agent: Applebot-Extended
Allow: /

# Meta
User-agent: meta-externalagent
Allow: /

User-agent: FacebookBot
Allow: /

# Common Crawl (feeds many open models)
User-agent: CCBot
Allow: /

# Amazon
User-agent: Amazonbot
Allow: /

# Mistral
User-agent: MistralAI-User
Allow: /

# DuckDuckGo
User-agent: DuckAssistBot
Allow: /

# Cloudflare's own renderer (used by AI Search / browser rendering)
User-agent: CloudflareBrowserRenderingCrawler
Allow: /

# ---------------------------------------------------------------------------
Sitemap: https://markdownstudio.dev/sitemap.xml
